{
  "name": "swagger-model",
  "version": "2.0.53",
  "description": "",
  "main": "generator.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/randing89/swagger-model"
  },
  "scripts": {
    "postinstall": "npm install -g istanbul",
    "test": "istanbul cover ./node_modules/mocha/bin/_mocha test/test.js --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "author": "Ran Ding",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^0.16.3",
    "handlebars": "^2.0.0",
    "lodash": "^3.0.0"
  },
  "devDependencies": {
    "chai": "^1.10.0",
    "coveralls": "^2.11.2",
    "mocha": "^2.1.0",
    "mocha-lcov-reporter": "0.0.1"
  }
}
